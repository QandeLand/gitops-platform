replicaCount: 2

image:
  repository: ghcr.io/qandeland/gitops-platform/backend
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 5000

resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 64Mi

env:
  APP_VERSION: "1.0.0"

